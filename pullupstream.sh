#!/bin/sh

# run the next line once to add the upstream remote
# git remote add upstream https://github.com/joshdholtz/JHAccordion.git

git fetch origin -v
git fetch upstream -v
#git merge upstream/master
#git merge upstream/cleaning-up

